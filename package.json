{
  "name": "platform-telega",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@9.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "pnpm -r --filter ./apps/* run build",
    "lint": "pnpm -r run lint",
    "dev": "pnpm --filter @telega/web dev"
  }
}
