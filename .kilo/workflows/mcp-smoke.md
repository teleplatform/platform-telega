# Workflow: MCP Smoke

Task:
1. Verify the local MCP stdio entry file path
2. Verify the built JS output path
3. Run a minimal MCP smoke if safe
4. Confirm whether initialize, tools/list, and one tools/call path are operational
5. Report exact evidence only

Expected output:
- checked files
- command(s) run
- observed pass/fail signals
- precise blockers if present