export function selectSurface(channel) {
    if (channel === "web") {
        return "web";
    }
    if (channel === "telegram") {
        return "telegram";
    }
    if (channel === "alice") {
        return "alice";
    }
    return "voice";
}
//# sourceMappingURL=surfaceSelector.js.map